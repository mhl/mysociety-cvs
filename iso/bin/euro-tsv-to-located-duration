#!/usr/bin/perl -w

use strict;

# Skip heading
<>;

while (<>) {
    chomp;
    my ($geonameid, $code, $name, $population, $wikipedia, $lat, $lon, $seconds, $rest_of_line) = split /\t/;

    if ($lat && $lon && $seconds) {
        print "$lat $lon $seconds\n";
    }
}

